#include <iostream>

using namespace std;

void f(int *p_array, size_t size){

       delete [] a;
    }

int main()
{
    int a = 5;
    char s[] = "Hello";

    int *a = new int [n];

    int *a = new int [n*m];
    a[x*m+y];
    int  **a = new int *[n];
    for (int i=0; i<n;++i){
        a[i] = new int [m];
    }
    a[i][j];
    int *a = new int [n*m];
    int **matrix = new int *[n];
    for (int i=0; i<n;++i){
        matrix[i] = a + i*m
    }
    matrix[i][j]

    int a[]={1,2,3};

    *(a+1) == a[1];
    *(int *)&s;
    int * Pa = a;

    return 0;
}
